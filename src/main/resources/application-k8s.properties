spring.cloud.kubernetes.reload.enabled=true
#spring.cloud.kubernetes.reload.strategy=restart_context
#spring.cloud.kubernetes.reload.mode=polling

management.endpoint.health.enabled=true
management.endpoint.info.enabled=true
management.endpoint.restart.enabled=true
ribbon.eureka.enabled=false
ribbon.eager-load.enabled=true

#config
#rabbit.username=${RABBIT_USER}
#rabbit.password=${RABBIT_PASSWORD}
#rabbit.address=${RABBIT_ADDRESS}

##database=mysql
#spring.datasource.driverClassName=com.mysql.jdbc.Driver
#spring.datasource.url = jdbc:mysql://${DB_HOST}/becastle?useSSL=false
#spring.datasource.username=${DB_USER}
#spring.datasource.password=${DB_PASSWORD}
#
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
#spring.jpa.hibernate.ddl-auto = update
#
#### Hibernate Logging
#logging.level.org.hibernate.SQL= DEBUG


security.oauth2.client.clientId=${CLIENT_ID}
security.oauth2.client.clientSecret=${CLIENT_SECRET}
security.oauth2.client.authorized-grant-types=${GRANT_TYPES}
security.oauth2.client.scope=${SCOPE}



